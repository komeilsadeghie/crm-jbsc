# راهنمای عیب‌یابی

## مشکل: خطا در ورود

### مرحله 1: بررسی اجرای سرور بک‌اند

سرور بک‌اند باید روی پورت 3001 در حال اجرا باشد. برای بررسی:

1. یک ترمینال جدید باز کنید
2. به پوشه پروژه بروید:
```bash
cd "c:\Users\kami\Desktop\crm  media"
```

3. سرور بک‌اند را اجرا کنید:
```bash
npm run dev:server
```

باید پیام زیر را ببینید:
```
🚀 CRM Server running on port 3001
📊 API available at http://localhost:3001/api
Connected to SQLite database
Database initialized
```

### مرحله 2: بررسی دیتابیس

اگر دیتابیس ایجاد نشده است:

1. ترمینال را باز کنید
2. دستور زیر را اجرا کنید:
```bash
npm run init:db
```

باید پیام زیر را ببینید:
```
Database initialized successfully!
Default admin user created: admin / admin123
```

### مرحله 3: بررسی فایل دیتابیس

فایل دیتابیس باید در مسیر زیر باشد:
```
database/crm.db
```

اگر این فایل وجود ندارد، مرحله 2 را انجام دهید.

### مرحله 4: بررسی پورت‌ها

مطمئن شوید که:
- پورت 3001 برای بک‌اند آزاد است
- پورت 3000 برای فرانت‌اند آزاد است

اگر پورت‌ها در حال استفاده هستند:
1. برنامه‌های دیگر را ببندید
2. یا در فایل `.env` پورت را تغییر دهید

### مرحله 5: بررسی لاگ‌های سرور

وقتی سعی می‌کنید وارد شوید، در ترمینال سرور باید لاگ‌های زیر را ببینید:
```
POST /api/auth/login
```

اگر خطایی می‌بینید، آن را بررسی کنید.

## مشکلات رایج

### خطا: "ECONNREFUSED" یا "Network Error"
**علت:** سرور بک‌اند در حال اجرا نیست
**راه حل:** مرحله 1 را انجام دهید

### خطا: "نام کاربری یا رمز عبور اشتباه است"
**علت:** کاربر admin وجود ندارد یا رمز عبور اشتباه است
**راه حل:** مرحله 2 را انجام دهید

### خطا: "خطا در ارتباط با دیتابیس"
**علت:** دیتابیس ایجاد نشده یا مشکل در دسترسی
**راه حل:** مرحله 2 را انجام دهید

### خطا: "Cannot find module"
**علت:** وابستگی‌ها نصب نشده‌اند
**راه حل:**
```bash
npm install
cd client
npm install
```

## تست اتصال

برای تست اینکه سرور در حال اجرا است:

1. مرورگر را باز کنید
2. به آدرس زیر بروید:
```
http://localhost:3001/api/health
```

باید پیام زیر را ببینید:
```json
{
  "status": "ok",
  "message": "CRM API is running"
}
```

اگر این پیام را نمی‌بینید، سرور در حال اجرا نیست.

## راه‌اندازی کامل

برای راه‌اندازی کامل از ابتدا:

```bash
# 1. نصب وابستگی‌ها
npm install
cd client
npm install
cd ..

# 2. ایجاد دیتابیس
npm run init:db

# 3. اجرای سرور بک‌اند (در یک ترمینال)
npm run dev:server

# 4. اجرای فرانت‌اند (در ترمینال دیگر)
cd client
npm run dev
```

یا برای اجرای همزمان:
```bash
npm run dev
```

## اطلاعات ورود پیش‌فرض

- **نام کاربری:** `admin`
- **رمز عبور:** `admin123`

**⚠️ هشدار:** در محیط production حتماً این رمز عبور را تغییر دهید!



