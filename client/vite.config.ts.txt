import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000, // پورتی که کلاینت روی آن بالا می‌آید
    proxy: {
      "/api": {
        target: "http://localhost:3001", // پورت سرور Node (بک‌اند)
        changeOrigin: true,
        secure: false,
      },
    },
  },
});
