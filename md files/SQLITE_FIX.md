# رفع خطای sql_notadb

## مشکل
خطای `sql_notadb` معمولاً به این معنی است که SQLite نمی‌تواند دیتابیس را پیدا کند یا مشکلی در اتصال وجود دارد.

## تغییرات انجام شده

### 1. بهبود مدیریت مسیر دیتابیس
- اضافه شدن تابع `getDbPath()` برای مدیریت بهتر مسیر
- پشتیبانی از development و production
- استفاده از `process.env.DB_PATH` برای تنظیم دستی

### 2. ایجاد خودکار دیتابیس
- ایجاد خودکار پوشه `database/` در صورت عدم وجود
- ایجاد خودکار فایل دیتابیس در صورت عدم وجود
- استفاده از `OPEN_READWRITE | OPEN_CREATE` برای اطمینان از ایجاد دیتابیس

### 3. بهبود Error Handling
- اضافه شدن error handler به `initDatabase`
- لاگ‌های بهتر برای دیباگ
- نمایش مسیر دیتابیس در کنسول

### 4. بهبود لاگ‌ها
- نمایش مسیر دیتابیس
- نمایش وضعیت ایجاد پوشه و فایل
- پیام‌های واضح‌تر برای خطاها

## راه‌حل

اگر هنوز خطا می‌دهد:

1. **بررسی مسیر دیتابیس:**
   ```bash
   # در لاگ‌های سرور باید مسیر دیتابیس را ببینید:
   🗄️  Database path: ...
   ```

2. **ایجاد دستی دیتابیس:**
   ```bash
   npm run init:db
   ```

3. **بررسی فایل دیتابیس:**
   ```bash
   # فایل باید در این مسیر باشد:
   database/crm.db
   ```

4. **تنظیم مسیر دستی (در صورت نیاز):**
   ```env
   # در فایل .env
   DB_PATH=./database/crm.db
   ```

## تغییرات در کد

### `src/database/db.ts`
- بهبود `getDbPath()` برای مدیریت بهتر مسیر
- اضافه شدن `OPEN_READWRITE | OPEN_CREATE` به اتصال دیتابیس
- ایجاد خودکار پوشه و فایل دیتابیس
- بهبود error handling و لاگ‌ها

